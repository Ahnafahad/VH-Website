# 🏗️ Secure API Architecture

## Before (INSECURE) ❌

```
┌─────────────────────────────────────────────┐
│  Browser (vocab-quiz/page.tsx)             │
│                                             │
│  const apiKey = "AIzaSy..." ← EXPOSED! 🚨  │
│                                             │
│  fetch(gemini.com?key=apiKey) ──────────┐  │
└─────────────────────────────────────────┼──┘
                                          │
                                          ▼
                              ┌───────────────────┐
                              │  Google Gemini    │
                              │  API              │
                              └───────────────────┘

PROBLEM: API key visible in browser DevTools!
Anyone can steal and use your key! 💸
```

## After (SECURE) ✅

```
┌────────────────────────────────────┐
│  Browser (vocab-quiz/page.tsx)    │
│                                    │
│  fetch('/api/generate-questions')  │ ← NO KEY!
│                                    │
└─────────────┬──────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────┐
│  Backend API Route                          │
│  src/app/api/generate-questions/route.ts    │
│                                             │
│  1. ✅ Check authentication (NextAuth)      │
│  2. ✅ Check authorization (access list)    │
│  3. ✅ Get key from env: process.env.KEY    │
│  4. ✅ Call Gemini API with key            │
│  5. ✅ Return response to frontend         │
└─────────────┬───────────────────────────────┘
              │
              ▼
┌───────────────────────────────────┐
│  .env.local (SERVER-SIDE ONLY)    │
│                                   │
│  GOOGLE_GEMINI_API_KEY=secret123  │ ← PROTECTED! 🔒
│                                   │
└─────────────┬─────────────────────┘
              │
              ▼
        ┌───────────────────┐
        │  Google Gemini    │
        │  API              │
        └───────────────────┘

SECURE: API key never leaves the server! ✨
Protected by authentication & authorization! 🛡️
```

## Security Layers

```
┌──────────────────────────────────────────┐
│  Layer 1: NextAuth Authentication        │
│  ✅ User must be logged in               │
└──────────────┬───────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────┐
│  Layer 2: Access Control List            │
│  ✅ User must be in authorized list      │
└──────────────┬───────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────┐
│  Layer 3: Environment Variables          │
│  ✅ API key stored server-side only      │
└──────────────┬───────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────┐
│  Layer 4: .gitignore Protection          │
│  ✅ .env.local never committed to git    │
└──────────────────────────────────────────┘
```

## API Flow Diagram

```
User Opens Quiz
      │
      ▼
┌─────────────────┐
│ Select Sections │
│ Click "Start"   │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────┐
│ generateQuestions()         │
│ Builds prompt               │
└────────┬────────────────────┘
         │
         ▼
┌──────────────────────────────────────┐
│ fetch('/api/generate-questions', {   │
│   method: 'POST',                    │
│   body: JSON.stringify({ prompt })   │
│ })                                   │
└────────┬─────────────────────────────┘
         │
         ▼
┌───────────────────────────────────────────┐
│ Backend API Route                         │
│ /api/generate-questions/route.ts          │
│                                           │
│ 1. getServerSession() → Check auth       │
│ 2. isEmailAuthorized() → Check access    │
│ 3. process.env.KEY → Get key securely    │
│ 4. fetch(gemini.com) → Call API          │
│ 5. return response → Send to frontend    │
└────────┬──────────────────────────────────┘
         │
         ▼
┌─────────────────────┐
│ Parse Questions     │
│ Display to User     │
└─────────────────────┘
```

## Environment Variables Setup

```
Project Root
│
├── .env.local ← CREATE THIS (gitignored)
│   └── GOOGLE_GEMINI_API_KEY=your_actual_key
│
├── .env.local.example ← TEMPLATE (safe to commit)
│   └── GOOGLE_GEMINI_API_KEY=your_actual_api_key_here
│
└── .gitignore ← UPDATED
    └── .env*.local ← Protects secrets
```

## Request/Response Flow

```
Frontend                Backend                 Gemini API
   │                       │                        │
   │ POST /api/...         │                        │
   │──────────────────────>│                        │
   │                       │                        │
   │                       │ Validate Auth          │
   │                       │────────┐               │
   │                       │        │               │
   │                       │<───────┘               │
   │                       │                        │
   │                       │ POST with key          │
   │                       │───────────────────────>│
   │                       │                        │
   │                       │                 ┌──────┴──────┐
   │                       │                 │ Generate    │
   │                       │                 │ Questions   │
   │                       │                 └──────┬──────┘
   │                       │                        │
   │                       │      JSON Response     │
   │                       │<───────────────────────│
   │                       │                        │
   │      JSON Response    │                        │
   │<──────────────────────│                        │
   │                       │                        │
   │ Parse & Display       │                        │
   │─────┐                 │                        │
   │     │                 │                        │
   │<────┘                 │                        │
```

## Benefits of This Architecture

✅ **Security**
- API key never exposed to client
- Multiple authentication layers
- Protected environment variables

✅ **Maintainability**
- Centralized API logic
- Easy to update/monitor
- Clear separation of concerns

✅ **Scalability**
- Add rate limiting easily
- Monitor API usage
- Cache responses if needed

✅ **Developer Experience**
- Simple frontend code
- Type-safe with TypeScript
- Good error handling

---

**Status:** ✅ Implemented and ready for API key
**Next Step:** Add your new Gemini API key to `.env.local`
